<?xml version="1.0" encoding="utf-8" ?>
<MasterDetailPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="appEconomEasy.Menu"
             IsPresented = "True";
             Title ="Menu">

    <MasterDetailPage.Master>
        <ContentPage Title="menuDeOpcoes">
            <ListView x:Name="listaMenu" ItemSelected="selecaoItens" BackgroundColor="#C4C4C4" Opacity="0.20"
                <ListView.ItemTemplate >
                    <DataTemplate>
                    <TextCell Text="{Binding nomeCampo}"
                              TextColor="Black">
                    </TextCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </ContentPage>
    </MasterDetailPage.Master>

    <MasterDetailPage.Detail>
        <ContentePage>

        </ContentePage>
    </MasterDetailPage.Detail>
    
</MasterDetailPage>